# 2026-04-26 작업 일지

## 오늘 한 것

- 학생 피드백 요청 화면에 "파일 확인 후 업로드" 안내문 초안 작성(최종본 확인, 암호 여부, 파일명 재확인)
- 업로드 API에서 `multipart` 파일명 인코딩 이슈 검토: `file.originalname` 단독 사용 시 깨짐 가능성 확인
- 클라이언트에서 `originalFilename` 폼 필드를 함께 전송하고 서버가 이를 우선 사용하는 방식으로 개선 방향 확정
- 첨부 목록 UI에서 실제 표시명이 사용자 로컬 파일명과 일치하는지 점검 기준 문서화

## 오류 / 어려웠던 점

- 브라우저/OS 조합에 따라 업로드 헤더 인코딩 결과가 달라, 로컬 한 환경만으로는 재현이 불안정했음

## 레퍼런스

- [RFC 7578 - multipart/form-data](https://www.rfc-editor.org/rfc/rfc7578)

## 내일 할 일

- `feedback/upload` 응답 `filename` 안전 처리 유틸 반영
- 프론트 `lib/api.ts`, `services/api.js` 양쪽 업로드 경로 모두 동일하게 수정
