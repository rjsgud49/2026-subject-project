# 2026-03-21 작업 일지

## 오늘 한 것

**TSX 전환 (JSX → TypeScript)**
- `tsconfig.json`, `vite-env.d.ts` 추가, `index.html` 엔트리 포인트 수정
- `package.json`에 `typescript`, `@types/react`, `@types/react-dom` 추가
- 전체 페이지·컴포넌트·레이아웃 파일 `.jsx` → `.tsx` 변환

**UX 개선**
- 로그인·회원가입 폼 `autoComplete="off"` 적용, 초기값 제거
- 장바구니 담기 성공 시 커스텀 팝업 표시 (`Modal` 컴포넌트 신규 구현)
  - "계속 쇼핑" / "장바구니로 이동" 두 버튼 제공

## 오류 / 어려웠던 점

- PowerShell에서 `&&` 미지원으로 `npm install` 실패 → `;` 로 대체
- sandbox 환경에서 `npm run build` 시 `EPERM` 오류 → 권한 설정 후 해결

## 레퍼런스

- [Vite + React + TypeScript 가이드](https://vitejs.dev/guide/)
- [TypeScript tsconfig 옵션](https://www.typescriptlang.org/tsconfig)

## 내일 할 일

- 구매한 강의 목록 숨김 처리
- `.gitignore` 정비 및 불필요 추적 파일 제거
