# 2026-04-21 작업 일지

## 오늘 한 것

- **백엔드 빌드**: `nest build` 클린 후 `teacher` 모듈에 `Enrollment` 엔티티 주입 변경이 다른 모듈과 충돌 없는지 확인
- **DB**: 개발 환경 `synchronize`로 `view_count`, `student_attachments_json`, 정산 컬럼 생성 여부 로그로 확인(운영은 마이그레이션 필요)
- **업로드**: `P2_UPLOAD_DIR`를 임시 폴더로 바꿔 저장·`GET /api/v1/files/:name` 재생/다운로드 한 사이클 테스트
- **피드백 DTO**: `CreateFeedbackDto`의 `attachments` 중첩 검증(`class-transformer` `Type`)이 빈 배열·미전송 시에도 통과하는지 확인

## 오류 / 어려웠던 점

- Windows에서 `P2_UPLOAD_DIR` 경로에 공백이 있을 때 `resolve` 결과가 기대와 다른 경우 → `.env`에서 따옴표·이스케이프 규칙 정리

## 레퍼런스

- [dotenv README](https://github.com/motdotla/dotenv#readme)

## 내일 할 일

- 운영 배포 시 `NODE_ENV=production`에서 `synchronize: false`일 때 필요한 `ALTER TABLE` 목록 초안 작성
- 강사 피드백 목록에 “첨부 있음” 표시가 모바일에서 줄바꿈될 때 레이아웃 깨짐 여부 확인
