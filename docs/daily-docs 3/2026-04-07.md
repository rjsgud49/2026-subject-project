# 2026-04-07 작업 일지

## 오늘 한 것

- 주요 페이지(`CourseList`, `InstructorProfile`, `FeedbackHistory`) 반응형 최소 너비·그리드 `minmax` 조정
- 버튼·링크에 `focus-visible` 스타일 보강, 스켈레톤 로딩 영역에 `aria-busy` 적용 검토
- `EmptyState` 문구를 화면별로 구체화(검색 결과 없음 vs 수강 중만 남은 경우)

## 오류 / 어려웠던 점

- 동일한 회색 대비가 WCAG 기준에 미달할 수 있는 구간을 디자인 토큰에서 한 단계 조정

## 레퍼런스

- [WCAG 개요](https://www.w3.org/WAI/WCAG21/quickref/)

## 내일 할 일

- 운영 배포 시 `users.bio` 마이그레이션 SQL 초안 작성
- `.env.example`에 DB·CORS·업로드 경로 변수 주석 보강
