# 2026-04-19 작업 일지

## 오늘 한 것

- **정산 계좌**: `/teacher/settlement` 저장 후 `auth/me` 응답과 Redux `user` 동기화 재확인, 빈 문자열 전송 시 DB `null` 반영 확인
- **강사 헤더 분기**: 비로그인·학생·강사 각각에서 `MainLayout` 네비게이션 노출 차이 수동 검증
- **피드백 리다이렉트**: 강사로 `/feedback`, `/feedback/buy` 직접 입력 시 `/teacher/feedback`으로 이동하는지 확인
- **코드 읽기**: `FilesController` 경로 traversal 방지(`resolve` + prefix 검사) 재검토

## 오류 / 어려웠던 점

- 주말/휴일 기준으로 팀 리뷰가 없어 이슈 트래킹은 자가 체크리스트로 대체

## 레퍼런스

- [OWASP Path Traversal](https://owasp.org/www-community/attacks/Path_Traversal)

## 내일 할 일

- `CoursePublic`에 `view_count` 노출 여부(강의 목록 카드 등) 제품 요구에 맞춰 추가할지 결정
- ESLint warning 정리(시간 남으면)
