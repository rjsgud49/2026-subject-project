PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=p1_interview
NODE_ENV=development

# DB 연결 실패(ECONNREFUSED) 시: PostgreSQL이 설치·실행 중인지 확인하세요.
# Windows: 서비스에서 postgresql 시작 또는 pgAdmin으로 서버 실행
# DB 생성: psql -U postgres -c "CREATE DATABASE p1_interview;"
