# 2026-04-14 작업 일지

## 오늘 한 것

- 페이지네이션: 마지막 페이지·단일 페이지·필터 변경 후 `page=1` 리셋 동작 회귀 테스트
- 검색어 `q`는 “필터 적용” 버튼 후에만 API 반영되는 현재 UX를 팀 내 공유(자동 검색 도입 시 부하·디바운스 논의)
- `uploads/feedback` 등 업로드 경로가 `.gitignore` 정책과 맞는지 확인(불필요한 바이너리 커밋 방지)

## 오류 / 어려웠던 점

- 필터 적용 없이 검색어만 바꾼 뒤 “목록이 안 바뀐다”는 오해 가능 → UI에 ‘적용’ 필요성을 보조 문구로 넣을지 결정 보류

## 레퍼런스

- [React Router useSearchParams](https://reactrouter.com/en/main/hooks/use-search-params)

## 내일 할 일

- 통합 시나리오 1회 더: 로그인(또는 데모 유저) → 강의 수강 → 피드백 이용권 사용
- 코드 상 미사용 import·경고 정리 PR 후보 분리
