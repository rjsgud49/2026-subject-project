services:
  postgres:
    image: postgres:16-alpine
    container_name: p3-lms-postgres
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: p2_lms
    volumes:
      - p3_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d p2_lms']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  p3_pg_data:
