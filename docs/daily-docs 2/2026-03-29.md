# 2026-03-29 작업 일지

## 오늘 한 것

**백엔드 요청 벨리데이션 정비**
- `main.ts`에서 전역 `ValidationPipe` 옵션 정리(whitelist 등)
- **인증**: `login.dto`, `signup.dto` 및 `auth.controller` 입력 검증
- **장바구니**: `add-cart.dto`, `cart.controller`
- **강의**: `course-query.dto`, `courses.controller`
- **Q&A**: 질문·답변 생성/수정 DTO, `questions.controller`

**프론트**
- `Login.tsx`, `Signup.tsx`에서 서버 검증 오류 메시지와 맞추기 위한 필드·제출 흐름 점검

## 오류 / 어려웠던 점

- 쿼리스트링용 DTO는 `IsNumberString` 등 타입과 메시지를 맞추지 않으면 400 응답만 나고 원인 파악이 어려움 → DTO에 `message` 명시

## 레퍼런스

- [class-validator](https://github.com/typestack/class-validator)

## 내일 할 일

- 강의·엔티티 관련 런타임 오류 재현 및 수정
- 프론트 폼 에러 토스트/인라인 표시 일관화
