# 2026-03-22 작업 일지

## 오늘 한 것

- 로그인 사용자의 이미 구매한 강의를 전체 목록에서 숨김 처리
  - Redux `enrollments` 상태 기반으로 `visibleItems` 필터링
  - 모든 강의가 필터링된 경우 안내 `EmptyState` 표시
- 루트 `.gitignore` 신규 추가 → `node_modules`, `dist`, `.env` 등 제외
  - `git rm -r --cached` 로 기존 추적 파일 캐시 제거
- `p1/README.md` 프론트엔드 스택에 TypeScript(TSX) 명시
- `개발계획서.md` Node.js 버전 `22 LTS` → `20 LTS` 정정

## 오류 / 어려웠던 점

- `.gitignore`만 추가해서는 이미 추적된 파일이 제외되지 않음
  → `git rm --cached` 를 별도로 실행해야 한다는 점을 다시 확인
- 개발계획서와 실제 환경 간 버전 불일치 → 문서 직접 수정으로 해결

## 레퍼런스

- [git rm --cached](https://git-scm.com/docs/git-rm)

## 내일 할 일

- 강의 카드 호버 UX 개선
- 영상 종료 시 다음 영상 이동 팝업 구현
