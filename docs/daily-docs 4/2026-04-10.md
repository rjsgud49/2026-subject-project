# 2026-04-10 작업 일지

## 오늘 한 것

**강의 목록 필터·표시 개선**
- `CourseQueryDto`의 `difficulty` 검증을 DB·UI와 맞게 완화(한글 난이도 `초급/중급/고급` 등 허용)
- 시드에 `interviewType` 추론(`inferInterviewType`) 적용, 기존 NULL 행 백필로 면접 방식 필터가 빈 결과가 나오지 않도록 정리
- `CourseList`: 수강 중인 강의를 목록에서 숨기지 않고 유지, `CourseCard`의 `isEnrolled` 뱃지로 구분
- 마운트 시 항상 `fetchEnrollments()` 호출해 수강 여부 뱃지가 비로그인·데모 유저 흐름에서도 맞도록 조정

**개발 환경·UX**
- Vite 프록시 `ECONNREFUSED` 시 사용자 안내: 백엔드 `npm run start:dev`(포트 3000) 또는 `VITE_USE_MOCK=true` 안내 문구 추가

**문서**
- `daily-docs 4` 4주차(04-10 ~ 04-16) 일지 작성 착수

## 오류 / 어려웠던 점

- 프론트만 켜 두면 `/api` 프록시가 거절되어 필터·목록이 전부 실패한 것처럼 보임 → 원인을 에러 영역에 명시해 디버깅 시간 단축
- DB에 `interview_type`이 비어 있으면 면접 방식 칩 필터가 항상 0건 → 시드·백필로 데이터 측을 먼저 맞춤

## 레퍼런스

- [NestJS Validation](https://docs.nestjs.com/techniques/validation)
- [Vite 서버 프록시](https://vitejs.dev/config/server-options.html#server-proxy)

## 내일 할 일

- 강사 프로필·강의 목록 연동 구간 재스모크(필터 조합별)
- P1 기획서 대비 누락 기능·버그 백로그 1차 정리
