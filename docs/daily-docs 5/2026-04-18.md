# 2026-04-18 작업 일지

## 오늘 한 것

- **환경 점검**: `p2/backend/.env`의 `PORT`와 `p2/frontend/vite.config.ts` 프록시 `target` 포트 일치 여부 확인 및 로컬 기동 스크립트 정리
- **스모크 테스트 (강사)**: 로그인 → 대시보드 수치 표시 → 내 강의 카드에서 수정 진입 → `CurriculumEditor` 저장 → 강의 영상 업로드 후 반환 URL을 커리큘럼에 반영
- **스모크 테스트 (학생)**: `/student/feedback/new`에서 파일 첨부 후 생성 → 강사 `/teacher/feedback`에서 첨부 링크 열기 확인
- **Git**: `uploads/` 하위 바이너리가 스테이징되지 않는지 `git status` 재확인

## 오류 / 어려웠던 점

- 브라우저 캐시로 이전 번들이 남아 라우트 변경이 안 보일 때 — 강제 새로고침 또는 dev 서버 재시작으로 해소

## 레퍼런스

- [Vite server.proxy](https://vitejs.dev/config/server-options.html#server-proxy)

## 내일 할 일

- 대시보드 `gross_revenue` 계산이 “현재 강의가 × 수강 수” 단순 모델임을 문서·UI 문구로 한 줄 명시할지 검토
- 관리자 계정으로 `/cart` 진입 시 안내 문구 UX 소폭 다듬기
