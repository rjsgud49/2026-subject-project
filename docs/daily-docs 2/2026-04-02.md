# 2026-04-02 작업 일지

## 오늘 한 것

**피드백·인프라**
- 피드백 제출 백엔드(`feedback` 모듈, `FeedbackSubmission` 엔티티, 업로드 파일 저장)와 프론트 연동
- `docs/p1/2026-04-09피드백.md`, `docs/디자인.md` 보강

**UI·페이지**
- 랜딩, 대시보드, 강의 상세·목록, 장바구니, 결제 완료, 로그인/회원가입 등 레이아웃·컴포넌트 정리
- `CourseThumbnail`, `CourseCard`, `FilterSidebar`, `EmptyState` 등 공통 UI 다듬기
- `api.js` 피드백 FormData 요청 분리

**강사 프로필**
- `GET /courses?instructor_name=` 로 강사별 강의 조회, `users.bio`·`instructor_meta`(소개, 카테고리 목록, 전체 강의 수) 응답
- 강사 페이지 상단에 **사용 카테고리 칩(전체 + 분야)** , 목록 **페이지당 12건** 페이지네이션
- 일반 검색 키워드 `q`에 강사명 부분 일치 포함

## 오류 / 어려웠던 점

- 기존에 강사 프로필이 제목 검색(`q`)만 써서 강사명이 제목에 없으면 목록이 0건으로 보이던 문제 → 전용 쿼리 파라미터로 분리해 해결
- 강사별 카테고리·전체 개수는 목록이 비어도 메타로 내려주도록 별도 집계 쿼리 추가

## 레퍼런스

- [인프런](https://www.inflearn.com/)
- [유데미](https://www.udemy.com/)

## 내일 할 일

- P1 문서와 API 최종 대조, E2E 시나리오(검색 → 상세 → 장바구니 → 수강) 점검
- 프로덕션 DB에 `users.bio` 컬럼 마이그레이션 여부 확인
