# 2026-04-22 작업 일지

## 오늘 한 것

- **UI 폴리시**: `TeacherWorkspace` 좁은 화면에서 사이드바 스택 배치(`@media`) 재확인
- **접근성**: 피드백 첨부 링크에 `target="_blank"` + `rel="noopener noreferrer"` 적용 여부 점검
- **성능**: 대시보드 API 한 번 호출로 목록·통계를 같이 쓰는 구조라 중복 `GET /teacher/courses` 호출이 없는지 확인
- **백로그 정리**: 과제 범위 대비 “나중에” 항목(결제 PG, 정산 배치 등)을 메모에만 남기고 코드 변경은 보류

## 오류 / 어려웠던 점

- 데모 영상 용량이 크면 로컬 업로드 테스트가 느려짐 → 작은 `webm` 샘플로 반복 테스트

## 레퍼런스

- [MDN: rel=noopener](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/rel/noopener)

## 내일 할 일

- 주간 마무리: `daily-docs 5` 7일분 교차 검토(날짜·링크·오타)
- 제출 전 최종 `npm run build`(프론트·백엔드) 한 번 더 실행
